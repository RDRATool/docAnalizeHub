# ED_T_BT018_B01_カスタム作業 - 1． CUSTOM媒体番号明細情報存在チェック処理

> ※このシートは図形で構成されているため、LM Studio(google/gemma-4-31b-qat)が図形シートのPNG画像から内容を再構成したものです。

# 1. 当日分のCUSTOM媒体番号明細情報wkbファイル存在チェック処理

## 1-1. CUSTOM媒体番号明細情報wkbファイルの検索
下記の条件によってCUSTOM媒体番号明細情報wkbファイルを検索する。

| 項目 | 内容 |
| :--- | :--- |
| 場所 | $TPC_HOME/upload/[機能部]/[機能ID]/[カスタムID]/[期別群]/$TPC_HOME/upload/[機能部]/[機能ID]/ |
| 取得 | 格納絶対パス（ファイル名を含む） |
| 条件 | ・フォルダ名には'ZZZZZZZZ'を含まない<br>・かつ ファイル名には'csvwkb'を含む<br>・かつ ファイル名には環境変数．期群を含む<br>・かつ ファイル更新日付が当日 |

### 取得ファイル数が0件の場合
NGを設定し、以下のメッセージをエラー情報に設定し、「4．終了処理」を行う。

| メッセージID | 埋込文字（%1） | 埋込文字（%2） |
| :--- | :--- | :--- |
| TPC_L_E_0011 | カスタム作業 | CUSTOM媒体番号明細情報wkbファイル |

### 上記以外の場合
取得格納絶対パスを当日分CUSTOM媒体番号明細情報wkbファイルリストに設定する。